# ---------------------------------------------------------------------------
# Theos build output
# ---------------------------------------------------------------------------
.theos/
packages/

# ---------------------------------------------------------------------------
# Packaging artifacts (build.sh)
#   - Facebook-Plus.deb : the renamed package build.sh produces
#   - package/          : legacy output directory name, kept ignored for safety
# ---------------------------------------------------------------------------
Facebook-Plus.deb
package/

# ---------------------------------------------------------------------------
# Editor / IDE support
#   compile_commands.json is generated by `make compile-commands`; it holds
#   absolute paths to whoever's theos install built it, so it is per-machine.
#   .clangd and .cache/ are that indexer's local state. .clangd.template is a
#   local scratch source `make compile-commands` consumes to generate .clangd and
#   then deletes, so it is never committed.
# ---------------------------------------------------------------------------
compile_commands.json
.cache/
.clangd
.clangd.template
.vscode/

# ---------------------------------------------------------------------------
# Python tooling (scripts/ and test/ helpers: svg2png.py, previews, etc.)
#   venv/         : virtualenv cairosvg renders from
#   pyrefly.toml  : per-user type-checker config
# ---------------------------------------------------------------------------
venv/
__pycache__/
*.py[cod]
.ruff_cache/
.mypy_cache/
.pytest_cache/
pyrefly.toml
test/*.py

# preview.py / preview_language.py render output
*_preview.png

# ---------------------------------------------------------------------------
# Test inputs
#   The decrypted Facebook IPA is copyrighted and machine-local — never commit it.
# ---------------------------------------------------------------------------
test/*.ipa

# ---------------------------------------------------------------------------
# Scratch space
#   draft/  : work-in-progress notes and mockups
#   .temp/  : scratch clones
# ---------------------------------------------------------------------------
draft/
.temp/

# ---------------------------------------------------------------------------
# OS cruft
# ---------------------------------------------------------------------------
.DS_Store
**/.DS_Store
Thumbs.db
