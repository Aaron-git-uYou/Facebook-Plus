// Diagnostics screen controller interface.

#import "FBPlus.h"

NS_ASSUME_NONNULL_BEGIN

/// Shows which hooks installed and which classes are loaded, so a problem can be
/// diagnosed from a screenshot rather than from a device console.
@interface FBPDiagnosticsController : UIViewController
@end

NS_ASSUME_NONNULL_END
